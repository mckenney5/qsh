#ifndef _USING_INTERPRETER_H
#define _USING_INTERPRETER_H

bool interpret(std::string cmd); // Exec's a program in the bin folders
std::string get_prompt(); //checks if a custom prompt is made, if not, return default

#endif
